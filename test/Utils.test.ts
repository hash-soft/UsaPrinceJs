//import Utils from '../src/Utils';

describe('compress', () => {
  it('compress', async () => {
    // アプリでは成功するがテストではBlob.stream()がundefinedになるのでスキップする
    //await Utils.compress('test');
  });
});
